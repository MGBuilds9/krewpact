// V2 uses sonner for toasts. This file exists for compatibility with
// UI components that import from '@/components/ui/use-toast'.
// In new code, import { toast } from 'sonner' directly.
export { toast } from 'sonner';
